name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  certify:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_DB: planora
          POSTGRES_USER: planora
          POSTGRES_PASSWORD: planora_ci
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U planora" --health-interval 5s --health-timeout 5s --health-retries 10
    env:
      DATABASE_URL: postgresql://planora:planora_ci@127.0.0.1:5432/planora?schema=public
      APP_ENV: test
      APP_URL: http://127.0.0.1:3000
      SESSION_COOKIE_NAME: planora_session
      SESSION_TTL_HOURS: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24, cache: npm }
      - run: npm ci
      - run: npm audit --audit-level=moderate
      - run: npx prisma generate
      - run: npm run db:migrate
      - run: npm run db:seed
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run test:financial
      - run: npm run test:excel
      - run: npm run test:security
      - run: npm run test:performance
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      - run: npm run db:reset:test
      - run: npm run db:seed
      - run: npm run test:e2e
